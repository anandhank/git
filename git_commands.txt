echo "# Java-Spring" >> README.md

create a new repository on the command line in local and add it to git hub

git init
git add README.md
git commit -m "first commit"
git branch -M master
git remote add origin https://github.com/anandhank/Java-Spring.git
git push -u origin master

if remote repository exists from the command line
git init
git add README.md
git commit -m "first commit"
git branch -M master
git push -u origin master

for further commits:

git add .
git commit -m "update commit"
git branch -M master
git push -u origin master
                
for storing pwd and user id of git
$ git config credential.helper store
$ git push http://example.com/repo.git
Username: <type your username>
Password: <type your password>
